services:
  - type: web
    name: boothflow
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: PORT
        value: "8081"
      - key: SPRING_PROFILES_ACTIVE
        value: prod
